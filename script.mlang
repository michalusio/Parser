string someFunctionName(string param1, SomeObject param2, int16 param3) {
  int16 a;
  int16 c = a;
}
// comments?
string anotherFunction() {
  string lmao;
  lmao[2] = invoke.test;
  /*
  * And block comments, too!
  */
  if (2)
  {
    invoke.test = do.something();
    lmao.test(invoke(), 5 + 3 + 8 * 2);
    a.b().c[2].d().x = "1" + "abc" - true + false;
  } else {
    a();
  }
}

type SomeObject {
  int32 id;
  int64 userId;
  string someData;
}